---
title: Dynamic Trust-Aware Sparse Communication Topology for LLM-Based Multi-Agent Consensus
title_zh: 基于大语言模型的多智能体共识中动态信任感知的稀疏通信拓扑
authors: "Wanshuang Gou, Zihan Liu"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01828v1"
tags: ["query:masrl"]
score: 9.0
evidence: 面向LLM多智能体共识的动态信任感知稀疏通信拓扑
tldr: 针对LLM多智能体系统中全连接通信导致开销随智能体数量平方增长、固定稀疏拓扑无法适应任务动态变化的问题，提出动态信任感知稀疏共识机制DySCo。该方法每轮依据智能体可靠性、答案分歧和任务相关性估计通信边价值，在预算约束下选择高价值边进行消息交换，并通过动态信任权重聚合答案，共识稳定后提前终止。在数学推理、逻辑推理和事实问答任务上验证，DySCo在显著降低通信开销的同时保留了关键的交叉验证信息，提升了多智能体共识的效率与适应性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01828v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 868, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01828v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1778, \"height\": 953, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01828v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 871, \"height\": 350, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01828v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 879, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01828v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 868, \"height\": 249, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01828v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1840, \"height\": 398, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01828v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 883, \"height\": 220, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01828v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 955, \"height\": 780, \"label\": \"Table\"}]"
motivation: 现有全连接通信导致消息数、Token开销和延迟随智能体数量平方增长，而固定稀疏拓扑无法适应不同任务及推理状态，易丢失关键纠错信息。
method: DySCo每轮基于可靠性、分歧和相关性估计边价值，预算约束下选高价值边通信，动态信任权重聚合答案，共识稳定后早停。
result: 在数学推理、逻辑推理、事实问答任务上，DySCo有效降低通信开销，同时保持或提升了共识准确性与收敛效率。
conclusion: 动态信任感知稀疏通信机制在控制开销的同时保证了关键信息交换，提升了多智能体系统的任务适应性和推理可靠性。
---

## 摘要
大语言模型驱动的多智能体系统通过多轮讨论、角色专长和交叉验证增强了复杂推理任务的可靠性。然而，现有的多智能体辩论和协作框架通常采用全连接通信，导致消息数量、Token成本和端到端延迟随智能体数量近似二次增长；尽管固定稀疏拓扑减少了开销，但它们无法根据不同的任务实例或中间推理状态调整通信关系，因此容易要么保留低价值交互，要么丢失关键的纠错信息。针对这一问题，本文提出了DySCo（动态稀疏共识），一种动态信任感知的稀疏共识机制。在每一轮推理中，DySCo基于智能体可靠性、答案分歧和任务相关性估计通信边的价值，并在预算约束下选择少量高价值边进行消息交换；然后通过动态信任权重聚合不同智能体的答案，并在共识稳定后提前终止讨论。该机制用按需通信替代了普遍广播，从而在保留必要交叉验证信息的同时降低了通信开销。我们进一步分析了通信复杂度和共识稳定性，并在数学推理、逻辑推理和事实问答任务上评估了DySCo的性能。

## Abstract
Large language model-driven multi-agent systems enhance the reliability of complex reasoning tasks through multi-round deliberation, role specialization, and cross-validation. However, existing multi-agent debate and collaboration frameworks typically adopt fully connected communication, causing the number of messages, token costs, and end-to-end latency to grow approximately quadratically with the number of agents; although fixed sparse topologies reduce overhead, they cannot adapt communication relationships to different task instances or intermediate reasoning states, making them prone either to preserving low-value interactions or to losing critical error-correction information. To address this problem, this paper proposes DySCo (Dynamic Sparse Consensus), a dynamic trust-aware sparse consensus mechanism. In each round of reasoning, DySCo estimates the value of communication edges based on agent reliability, answer divergence, and task relevance, and selects a small number of high-value edges for message exchange under budget constraints; it then aggregates the answers of different agents through dynamic trust weights and terminates the discussion early once consensus stabilizes. This mechanism replaces universal broadcasting with on-demand communication, thereby reducing communication overhead while preserving essential cross-validation information. We further present analyses of communication complexity and consensus stability, and evaluate the performance of DySCo on mathematical reasoning, logical reasoning, and factual question-answering tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有的基于大语言模型（LLM）的多智能体系统在协作推理时通常采用全连接通信拓扑，导致消息数、Token 成本和端到端延迟随智能体数量 \(n\) 近似平方级增长（\(O(Rn^2)\)）。虽然固定稀疏拓扑（如环图、随机图）可降低开销，但无法根据不同的任务实例或中间推理状态动态调整通信关系，容易保留低价值交互或丢失关键纠错信息。
- **研究背景**：近年来，LLM-based 多智能体系统（如 MAD、ChatDev、AutoGen、MetaGPT）通过多轮讨论、角色专长和交叉验证提升了复杂推理任务的可靠性。然而，通信拓扑的固定性与推理状态的动态性之间的不匹配成为瓶颈。作者旨在设计一种能自适应选择高价值通信边、平衡效率与质量的机制。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：提出 DySCo（Dynamic Sparse Consensus），一种动态信任感知的稀疏共识机制。它用按需通信替代无条件广播，在每轮推理中根据智能体的历史可靠性、当前置信度、答案分歧度、任务相关性及通信成本动态评估候选通信边的价值，在预算约束下选择 Top-k 高价值边进行消息交换；收到反馈后，智能体结合信任权重更新答案；最终通过信任加权投票聚合共识，并在共识熵低于阈值时提前终止。
- **关键技术细节**：
  - **状态定义**：每个智能体在轮次 \(t\) 的状态为 \(z_i^t = (y_i^t, r_i^t, c_i^t)\)，分别表示当前答案、自然语言推理、自报告置信度。
  - **边评分函数**：  
    \[
    s_{ij}^t = \alpha T_j^t + \beta c_j^t + \gamma D_{ij}^t + \delta H_{ij}(x) - \eta L_j^t
    \]  
    其中 \(T_j^t\) 是历史信任权重，\(c_j^t\) 是当前置信度，\(D_{ij}^t\) 是答案分歧（如是否一致），\(H_{ij}(x)\) 是任务依赖/角色互补性，\(L_j^t\) 是估计 Token 成本。
  - **邻居选择**：每个接收方 i 选取评分最高的 k 个发送方，即 \(N_i^t = \text{TopK}_{j \neq i}(s_{ij}^t, k)\)，总边数 ≤ \(nk\)。
  - **消息压缩**：发送方生成结构化压缩消息，包含当前答案、最关键的支撑理由、对接收方答案的潜在反例或修订建议，限制冗余长文本传播。
  - **信任更新**：  
    \[
    T_i^{t+1} = \lambda T_i^t + (1 - \lambda)(\rho V_i^t + (1 - \rho) A_i^t)
    \]  
    其中 \(V_i^t\) 是轻量验证器分数，\(A_i^t\) 是与主流共识的匹配度。
  - **共识聚合**：最终答案 \(\hat{y} = \arg\max_y \sum_{i=1}^n T_i^R c_i^R \mathbb{I}[y_i^R = y]\)。
  - **早停**：当加权答案分布熵低于阈值 \(\epsilon\)，或连续两轮共识不变且平均置信度增幅小于门限时，终止讨论。

### 3. 实验设计：数据集、基准、对比方法
- **数据集**：三个类别——数学推理（GSM8K）、逻辑推理（LogiQA）、常识/事实问答（StrategyQA）。每个数据集随机抽样 300 个测试实例。
- **基准模型**：统一使用 GPT-3.5-turbo。
- **多智能体配置**：默认 \(n=6\) 个智能体，最大通信轮数 \(R=3\)，DySCo 的邻居预算 \(k=2\)。
- **对比方法**：单智能体链式推理（Single-Agent CoT）、自一致性（Self-Consistency，\(n=6\) 采样聚合）、全连接多智能体辩论（Dense MAD）、固定环状拓扑（Static Ring）、随机选择 k 邻居（Random-k）、仅基于历史信任的稀疏拓扑（Trust-Only Sparse）。

### 4. 资源与算力
- **明确性**：论文未提及使用的 GPU 型号、数量或训练时长。仅说明基础模型为 GPT-3.5-turbo（通过 API 调用），因此实验为推理阶段，不涉及训练算力。

### 5. 实验数量与充分性
- **实验数量**：
  - **主实验**：在 3 个数据集上对比 7 种方法（含 DySCo），报告准确率、Token 成本、延迟、共识熵，并给出 3 次随机种子的均值和标准差。
  - **消融实验**：在 GSM8K 上移除信任权重、分歧评分、任务依赖、消息压缩、早停等 5 个模块，观察准确率和 Token 变化。
  - **可扩展性实验**：在 GSM8K 上设置 \(n=4,6,8,12\)，对比 Dense MAD 和 DySCo 的准确率与 Token 成本。
  - **共识动力学分析**：在 LogiQA 上展示 0-3 轮的共识熵与准确率演化。
  - **超参数敏感性分析**：改变邻居预算 \(k\)、最大轮数 \(R\)、早停阈值 \(\epsilon\) 以及边评分权重 \(\alpha,\beta,\gamma,\delta,\eta\)，观察性能变化（共 18 组设置）。
- **充分性与公平性**：实验较为充分，覆盖多种经典基线、消融、可扩展性和超参数分析。随机种子重复 3 次取均值标准差，对比公平。但数据集规模（每类 300 条）中等，未在更大规模（如 MATH、MMLU）上验证；仅使用 GPT-3.5-turbo，未在更强的模型（如 GPT-4）或开源模型上验证。

### 6. 论文的主要结论与发现
- **主要结论**：DySCo 在显著降低通信开销（Token 减少约 70%，延迟降低近一半）的同时，在三个数据集上均取得了优于 Dense MAD 的准确率（GSM8K 上 84.3% vs 82.0%），证明了动态稀疏通信可以有效保留纠错信息并提升推理质量。
- **其他发现**：
  - 可扩展性实验表明，DySCo 的 Token 成本随 \(n\) 近似线性增长（\(O(Rnk)\)），而 Dense MAD 呈二次增长；且 DySCo 的准确率随 \(n\) 增加持续提升（12 个智能体时达 86.8%），而 Dense MAD 在 \(n>8\) 时准确率停滞甚至下降。
  - 消融实验显示信任权重和分歧评分对性能提升最关键；消息压缩几乎不损失准确率（仅降 0.2%）但大幅降低 Token；早停机制可节省 25% Token 且不影响准确率。
  - 共识动力学分析显示 DySCo 比 Dense MAD 更快降低共识熵（第 1 轮 0.62 vs 0.88），并更早进入稳定状态。
  - 超参数敏感性分析表明 DySCo 对大部分参数不敏感，性能稳定。

### 7. 优点：方法或实验设计上的亮点
- **方法亮点**：
  - 将通信拓扑从固定结构变为动态决策变量，根据推理状态自适应选择高价值边，兼顾效率与准确性。
  - 多维度边评分（历史信任、置信度、分歧、任务依赖、成本）设计合理，且通过超参数可调。
  - 信任加权聚合与早停机制有效抑制噪声，降低冗余通信。
  - 消息压缩（结构化批判）进一步控制成本，且保留关键信息。
  - 提供可复现的提示模板（Solve、Critique、Revise、Consensus Normalization）。
- **实验亮点**：
  - 覆盖多种稀疏拓扑基线（环、随机、信任仅），消融全面，可扩展性验证充分。
  - 量化了 Token 成本、延迟、共识熵等多个维度，而不仅是准确率。
  - 超参数敏感性实验证明方法鲁棒性。

### 8. 不足与局限
- **实验局限**：
  - 仅测试 GPT-3.5-turbo，未在更先进模型（如 GPT-4、Claude、Llama 3）或开源模型上验证泛化性。
  - 数据集规模较小（各 300 条），且仅涵盖数学/逻辑/事实问答，未涉及开放式生成、价值判断、长文档推理等任务，这些任务中自报告置信度和一致性信号可能不可靠。
  - 仅使用 3 个随机种子，统计稳定性一般。
- **方法局限**：
  - 假设 LLM 自报告置信度可反映真实质量，但 LLM 存在校准问题（过度自信或低估），可能导致边选择偏差。
  - 信任权重可能产生“富者愈富”效应，使早期偶然正确的智能体主导后续讨论，压制少数派推理路径。
  - 过于稀疏的通信可能导致局部共识簇，需配合随机交叉边保证全局连通性（文中提到但未实验验证）。
  - 在具有多个有效答案的开放任务中，边评分中的分歧度可能反映表达差异而非实质互补，有效性受限。
  - 依赖外部验证器（数学检查、单元测试等）提升信任更新可靠性，但非所有任务都有可用验证器。

（完）
