---
title: "MOSS: Self-Evolution through Source-Level Rewriting in Autonomous Agent Systems"
title_zh: 自进化自主智能体系统中的源码级重写
authors: "Qianshu Cai, Yonggang Zhang, Xianzhang Jia, Huajiang Zheng, Wei Xue, Jun Song, Xinmei Tian, Yike Guo"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22794v2"
tags: ["query:agent-papers"]
score: 9.0
evidence: 自主智能体系统中的源码级重写自我进化
tldr: 现有自主代理系统部署后静态，仅在文本层进化，无法修复代码层结构缺陷。MOSS提出源码级自进化，通过自动收集生产故障证据、多阶段确定性重写流水线，将代码修改委托给外部编码代理，并利用临时试验器验证、用户同意与健康探针回滚机制安全部署。在OpenClaw上，四任务平均评分从0.25升至0.61，无需人工干预。该方法图灵完备、效果确定且不因上下文漂移退化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22794v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1326, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22794v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1299, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22794v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 980, \"height\": 294, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.22794v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1109, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.22794v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1013, \"height\": 322, \"label\": \"Table\"}]"
motivation: 现有自进化代理仅能修改文本工件，无法触及代码中的路由、钩子排序等结构缺陷，因此需要源码级适应。
method: MOSS自动收集生产失败批次，通过多阶段流水线确定重写方案，委托外部编码代理修改源码，并在临时试验器上验证后经用户同意与健康探针回滚部署。
result: 在OpenClaw基准上，MOSS单轮自进化将四任务平均得分从0.25提升至0.61。
conclusion: 源码级自我进化比文本级更通用、确定且鲁棒，能修复结构性问题并提升自主代理的持续学习能力。
---

## 摘要
自主智能体系统在部署后基本处于静态：它们无法从用户交互中学习，反复出现的故障会持续存在，直到下一次人工驱动的更新发布修复。自进化智能体应运而生，但所有进化都局限于文本可变的工件——技能文件、提示配置、记忆模式、工作流图——而智能体框架保持不变。由于路由、钩子排序、状态不变量和调度存在于代码而非任何文本工件中，文本层根本无法触及整类结构故障。我们认为源码级适配是一种本质上更通用的媒介：它是图灵完备的，是每个文本可变范围的严格超集，具有确定性效果而非依赖于基础模型的遵从性，并且不会在长上下文漂移下退化。我们提出了MOSS，一个在生产级智能体基座上执行源码级自重写的系统。每次进化都锚定于自动策划的生产故障证据批次，并通过确定性多阶段流水线进行；代码修改委托给可插拔的外部编码智能体CLI，而MOSS保留阶段排序和判定。候选方案通过将批次在临时试验工作器中的候选镜像上重播来验证，然后通过用户同意门控的原位容器交换和健康探测门控的回滚进行推广。在OpenClaw上，MOSS在单个周期内无需人工干预就将四项任务的平均评分从0.25提升到0.61。

## Abstract
Autonomous agentic systems are largely static after deployment: they do not learn from user interactions, and recurring failures persist until the next human-driven update ships a fix. Self-evolving agents have emerged in response, but all confine evolution to text-mutable artifacts -- skill files, prompt configurations, memory schemas, workflow graphs -- and leave the agent harness untouched. Since routing, hook ordering, state invariants, and dispatch live in code rather than in any text artifact, an entire class of structural failure is physically unreachable from the text layer. We argue that source-level adaptation is a fundamentally more general medium: it is Turing-complete, a strict superset of every text-mutable scope, takes effect deterministically rather than through base-model compliance, and does not erode under long-context drift. We present MOSS, a system that performs self-rewriting at the source level on production agentic substrates. Each evolution is anchored to an automatically curated batch of production-failure evidence and proceeds through a deterministic multi-stage pipeline; code modification is delegated to a pluggable external coding-agent CLI while MOSS retains stage ordering and verdicts. Candidates are verified by replaying the batch against the candidate image in ephemeral trial workers, then promoted via user-consent-gated, in-place container swap with health-probe-gated rollback. On OpenClaw, MOSS lifts a four-task mean grader score from 0.25 to 0.61 in a single cycle without human intervention.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有自主智能体系统（如OpenClaw）在部署后基本保持静态，无法从用户交互中学习，重复故障只能等待下一次人工更新修复。已有自进化智能体仅能修改文本可变工件（技能文件、提示配置、记忆模式、工作流图），但无法触及智能体框架（harness）层——即路由、钩子排序、状态管理、调度等代码实现的逻辑。这导致一类重要的结构性故障（如消息路由错误、钩子执行顺序混乱、会话状态损坏、并发技能原子性问题）完全无法被文本层的修改修复。

- **整体含义**：论文论证源码级适应是更通用的媒介，具有四个优势：图灵完备性、是文本可变范围的严格超集、效果确定（不依赖基础模型是否遵从新文本）、不会因长上下文漂移而退化。因此，论文提出MOSS系统，在生产级智能体基座上实现源码级自重写，将进化能力扩展到完整框架层。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
MOSS采用**有向进化**：每次进化锚定于一个自动收集的生产故障证据批次，后续所有阶段都固定于修复该批次，而非基于合成基准（benchmark）进行随机突变。

### 关键技术细节

1. **五部分系统架构**：
   - **主容器**：运行用户面对智能体的生产级基座（OpenClaw）。
   - **控制面CLI**：通过`moss evo`命令暴露整个进化生命周期（状态、批次、开始、停止、应用等），智能体可调用。
   - **外部编码智能体CLI**：可插拔的编码代理（支持Claude Code、OpenAI Codex、DeepSeek-TUI、OpenCode），负责实际代码编辑。
   - **主机守护进程（host-daemon）**：管理容器生命周期、编码代理调用、试验工作器、自动扫描引擎和交换监督。
   - **临时试验工作器**：每次迭代启动短生命周期容器，在候选镜像上重播批次任务以验证效果。

2. **进化流程（四层嵌套）**：
   - **层0**：预循环基线评估——对批次原始对话记录评分，锁定关键点集。
   - **层1**：迭代循环（迭代1→迭代2→...→迭代N），直到批次修复或判断无进展。
   - **层2**：每迭代的七阶段流水线：
     1. **定位**：读取基线轨迹和故障，撰写诊断。
     2. **计划**：确定根本原因并指定修复方案（文件、逻辑变更）。
     3. **计划评审**：质量门控，批准或拒绝计划，可循环。
     4. **实现**：在内部仓库中写入代码作为一次git提交。
     5. **代码评审**：质量门控，校准diff与计划。
     6. **任务评估**：对每个任务的关键点进行四等级评分（强/充分/弱/缺失）。
     7. **裁决**：综合评估给出四种判决之一：收敛、需更多工作、模型根本限制、架构根本限制。
   - **层3**：阶段内部重试循环（围绕计划评审和代码评审）。

3. **运行时验证**：编码后构建镜像，启动N个临时试验工作器，使用与故障批次相同的提示自主执行任务，暴露运行时问题。

4. **安全部署**：收敛后用户通过`moss evo apply`授权，主机守护进程执行原位容器交换（保持用户状态卷），随后进行90秒健康探测窗口，连续三次通过则提交交换，否则自动回滚至最后已知良好镜像。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：使用一个控制批次，包含4个来自`claweval`基准的任务（操作/合规审计类别）：
  - T141zh/T142：SLA合规审计（中文/英文）
  - T137zh/T138：补货链检查（中文/英文）
  每个任务分为两个语言变体，共4个任务。

- **基准**：`claweval`评分器（0-1尺度，0.75为通过阈值），作为外部数值见证。智能体基座为OpenClaw搭配DeepSeek V3.2模型。

- **对比方法**：论文未进行与其他方法的横向对比。主要展示MOSS自身单次迭代前后评分变化。对比对象为基线版本（当前部署版本）与迭代1后的候选版本。

## 4. 资源与算力

论文**未明确说明**实验使用的GPU型号、数量以及训练时长。仅提及使用了DeepSeek V3.2作为基础模型，编码代理CLI可插拔（如Claude Code、OpenAI Codex），但未披露算力资源细节。

## 5. 实验数量与充分性

- **实验数量**：核心实验仅为一个案例研究：单次迭代（单轮循环），包含4个任务，每个任务运行3次试验（trials）取平均分。
- **充分性**：
  - **积极面**：实验设计较为清晰，展示了从故障批次到修复部署的完整闭环。`claweval`评分器提供了可复现的度量。
  - **不足**：实验规模较小（仅4个任务），缺乏多元化任务领域的验证；未进行多轮迭代或消融研究（如不同编码代理、不同迭代次数、不同批次大小的影响）；未与基线系统（如Hermes Agent等仅文本级进化系统）进行直接对比；未测试生产环境下的长周期稳定性、用户反馈效果等。因此实验覆盖不够全面，公平性方面未与现有工作对比，证明力度有限。

## 6. 论文的主要结论与发现

- **主要结论**：MOSS通过源码级自重写，在单个进化周期内将OpenClaw在4个`claweval`任务上的平均评分从0.25提升至0.61（提高了0.3574），无需人工干预。最大提升来自T138（补货链检查英文）任务，从0.209跃升至0.9049。SLA任务提升至0.53-0.55区间，但仍未完全通过。变化源于框架层修改（添加注解分支和预调用拒斥门），而非提示或技能文件。

- **发现**：源码级进化能够修复文本层无法触及的结构性故障，方法具有确定性和稳定性，且部署过程通过用户同意和健康探测保证安全。

## 7. 优点

1. **创新性**：首次将自进化能力从文本工件扩展到智能体框架源码，解决了现有系统的根本局限。
2. **实用性**：面向生产环境设计，包括自动故障采集、用户同意门控、健康探测回滚等工业级机制，可部署于真实多通道服务。
3. **泛化性**：MOSS与基座的接口仅需五个原语（shell工具执行、文件系统读、周期性调度、webhook传递、系统提示注入），宣称可适配其他主流智能体系统。
4. **可靠性**：确定性多阶段流水线和可插拔编码代理分离了决策与编辑，质量门控和运行时验证减少了错误补丁风险。
5. **透明度**：所有工件（计划、diff、构建日志、评分矩阵）均可由用户按需审计，增强了可控性。

## 8. 不足与局限

1. **实验规模小**：仅4个任务，单次迭代，缺乏对复杂场景、多轮进化、跨任务的泛化验证。未展示在开放用户流量下的长期效果。
2. **无对比基线**：未与Hermes Agent、SkillClaw等其他自进化系统进行定量比较，未能直接证明源码级进化优于文本级进化。
3. **未披露计算成本**：编码代理调用、构建镜像、多次试验运行均消耗资源，论文未提供任何时间或成本数据，无法评估实际部署开销。
4. **依赖外部编码代理**：代码修改质量完全依赖于所选的编码代理CLI（如Claude Code），可能引入供应商锁定或模型能力瓶颈。
5. **局限在特定基座**：虽然声称可适配，但实验仅在OpenClaw上进行，其他基座的兼容性和效果未知。
6. **未处理用户隐私与安全**：自动扫描用户会话可能会引起数据隐私问题，论文未讨论相关防护措施。
7. **长周期影响未知**：多次源码重写可能引入代码膨胀、维护复杂性或不可预见副作用，论文未探索。

（完）
