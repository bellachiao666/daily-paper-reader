---
title: Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning
title_zh: 面向长周期智能体强化学习的群图策略优化
authors: "Yunan Wang, Minghui Song, Zihan Zhang, Shaohan Huang, Haizhen Huang, Furu Wei, Weiwei Deng, Feng Sun, Qi Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22995v1"
tags: ["query:masrl"]
score: 8.0
evidence: 长程智能体强化学习中的信用分配
tldr: "长程智能体强化学习面临奖励稀疏和延迟问题，现有步级方法仍采用线性轨迹视角，信用分配粗粒度。本文提出G2PO，将交互轨迹构建为全局状态转换图，通过组聚合状态值估计降低方差，并基于边的优势估计聚焦关键转换。在WebShop、ALFWorld和AppWorld基准上，G2PO大幅超越GRPO等基线，成功率提升高达22.2%。该工作为长程时序决策提供了图结构化的有效范式。"
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22995v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 393, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1447, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 555, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 699, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 728, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22995v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 832, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22995v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 590, \"height\": 323, \"label\": \"Table\"}]"
motivation: 现有方法忽略状态转换图结构，导致高方差状态值估计和短视信用分配。
method: G2PO将线性轨迹转化为全局图，采用组聚合状态值估计和边中心优势估计。
result: "在WebShop、ALFWorld、AppWorld上超越基线，成功率提升22.2%。"
conclusion: G2PO通过图结构化优化有效解决长程奖励稀疏问题。
---

## 摘要
基于群体的强化学习显著增强了智能体场景中的大语言模型。为了实现更细粒度的策略更新，最近的智能体强化学习框架已从轨迹级训练转向步骤级训练。然而，长周期智能体强化学习面临严重的奖励稀疏性和延迟问题，因为反馈通常延迟数十个交互步骤。虽然现有的步骤级框架细化了训练粒度，但其信用分配仍然是粗粒度的，并且仍将智能体探索视为孤立的线性轨迹。这种过于简化的视角忽略了状态转移固有的图结构，导致高方差的状态价值估计和短视、局部的信用分配。为了克服这些关键瓶颈，我们提出了群图策略优化（G2PO），一种专为多轮智能体任务设计的新型群体强化学习算法。G2PO明确地将线性交互轨迹转换为全局状态转移图。通过聚合不同轨迹中的相同观测，我们引入了群体聚合状态价值估计，从而减少了采样方差和轨迹依赖偏差。此外，我们重新定义了智能体动作作为状态节点之间的转移，并提出了一种以边为中心的优势估计策略。通过在整个图上全局标准化时序差分误差，G2PO明确识别并优先处理推动绝对任务进展的关键转移。在代表性的长周期基准测试——WebShop、ALFWorld和AppWorld上的大量实验表明，G2PO显著优于最先进的基于提示和强化学习的基线方法，相对于GRPO取得了高达22.2%的成功率提升。

## Abstract
Group-based Reinforcement Learning (RL) has significantly enhanced Large Language Models (LLMs) in agentic scenarios. To achieve finer-grained policy updates, recent agentic RL frameworks have shifted from trajectory-level to step-level training. However, long-horizon agentic RL suffers from severe reward sparsity and delay, as feedback is often deferred for dozens of interaction steps. While existing step-level frameworks refine training granularity, their credit assignment remains coarse-grained and still treats agent exploration as isolated, linear trajectories. This oversimplified perspective ignores the inherent graph structure of state transitions, leading to high-variance state-value estimation and myopic, localized credit assignment. To overcome these critical bottlenecks, we propose Group-Graph Policy Optimization (G2PO), a novel group-based RL algorithm tailored for multi-turn agentic tasks. G2PO explicitly transforms linear interaction trajectories into a global state-transition graph. By aggregating identical observations across different trajectories, we introduce group-aggregation state-value estimation that reduces sampling variance and trajectory-dependent bias. Furthermore, we redefine agent actions as transitions between state nodes and propose an edge-centric advantage estimation strategy. By globally standardizing Temporal Difference (TD) errors across the entire graph, G2PO explicitly identifies and prioritizes critical transitions that drive absolute task progress. Extensive experiments on representative long-horizon benchmarks-WebShop, ALFWorld, and AppWorld-demonstrate that G2PO substantially outperforms state-of-the-art prompt-based and RL baselines, achieving remarkable success rate improvements of up to 22.2% over GRPO.

---

## 论文详细总结（自动生成）

# 论文《Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning》详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：大语言模型（LLM）正从聊天机器人向智能体（Agent）演进，需要在多轮、长周期交互中做出决策（如电商、具身环境、软件生态）。强化学习（RL）已成为提升LLM推理和决策能力的标准后训练范式，但现有方法（如PPO、GRPO）主要针对单轮推理任务。
- **核心问题**：长周期智能体强化学习面临两大挑战：
    - **奖励稀疏与延迟**：最终奖励往往在数十步交互后才给出，中间步骤无即时反馈，导致信用分配困难。
    - **现有步骤级RL方法的缺陷**：当前步骤级训练（如GiGPO）虽然细化了训练粒度，但仍将智能体探索视为孤立、线性的轨迹，忽略了状态转移固有的图结构。这导致：
        1. **状态价值估计高方差**：同一状态在不同轨迹中可能导向不同结局，基于单条轨迹的估计不可靠。
        2. **局部组比较范围有限**：仅在同一状态下比较动作，无法区分“在琐碎状态中的微小改进”与“推动关键突破的动作”之间的全局重要性。
- **整体含义**：提出G2PO，通过将交互轨迹重构为全局状态转移图，实现更稳定、全局化的信用分配，从而显著提升长周期任务的性能。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 将采样的多条轨迹转换为**状态组图**（State Group Graph）：相同观测聚为节点，动作作为边。
- 基于图结构，提出**组聚合状态价值估计**（Group-Aggregation State-Value Estimation）和**边中心优势估计**（Edge-Centric Advantage Estimation），提供细粒度、低方差、全局视角的信用分配。

### 关键技术细节

#### 2.1 状态组图构建
- 对给定任务x，采样N条完整轨迹{τ_i}。
- 将所有观测O按相同或相似观测聚类为G个不相交的状态组{G_k}，每个组对应一个节点。
- 节点之间通过动作（边）连接：若存在从节点G_s到G_t的转移，则记录边(G_s, a, G_t)。
- 得到图{V, E}，V为节点集合，E为边集合。

#### 2.2 组聚合状态价值估计
- 对于每个轨迹中的步骤(j)，计算折扣回报：v_{ij} = γ^{T-j+1} R_i（R_i为最终奖励）。
- 对于每个状态组G_k，其价值定义为组内所有步骤价值的平均值：V(G_k) = (1/|G_k|) Σ_{o_{ij}∈G_k} v_{ij}。
- **优势**：降低方差（理论证明见论文附录B.1），并平滑未来步骤噪声。

#### 2.3 多粒度优势估计
- **节点中心优势**（Node-Centric Advantage, A_{NC}）：对于从同一节点G_k出发的所有动作，将其目标节点价值组成本地参考集Δ_k，然后计算每个动作的标准化优势：A_{NC}(a_{ij}) = (V(G_{k'}) - mean(Δ_k)) / std(Δ_k)。反映动作在局部状态下的相对优势。
- **边中心优势**（Edge-Centric Advantage, A_{EC}）：将每个动作视为边，计算TD误差（价值增益）：δ_{ij} = V(G_{k'}) - V(G_k)。然后对整个图中所有边的TD误差进行全局标准化：A_{EC}(a_{ij}) = (δ_{ij} - mean({δ})) / std({δ})。**关键创新**：捕捉动作对任务进度的绝对贡献，而非仅局部比较。
- **回合级优势**（Episode-Level Advantage, A_{EP}）：基于最终奖励R_i进行组标准化，与GRPO类似。
- **综合优势**：A_{ij} = A_{EP}(a_{ij}) + w·(A_{NC}(a_{ij}) + A_{EC}(a_{ij}))，w为平衡权重。
- **目标函数**：采用GRPO形式的截断代理目标，并加入KL散度惩罚。

## 3. 实验设计：数据集/场景、基准测试、对比方法

### 数据集/场景
- **WebShop**：电商环境，智能体需搜索、导航、选择商品属性以满足用户需求。
- **ALFWorld**：具身物理环境（文本版），包含6类家务任务：Pick、Look、Clean、Heat、Cool、Pick2。
- **AppWorld**：复杂软件生态系统，智能体通过Python API调用与多个App（如Spotify、Venmo）交互。

### Benchmark
- 各基准均报告**成功率**（Success Rate）或**得分**（Score）。WebShop同时报告得分和成功率；ALFWorld报告各子任务及总体成功率；AppWorld报告成功率和得分。

### 对比方法
- **前沿现成模型**：Qwen3.5-397B、DeepSeek-V3.2、Gemini-2.5-Pro。
- **基于提示的方法**：ReAct、Reflexion。
- **RL训练基线**：
    - PPO（带critic）
    - RLOO（留一法）
    - GRPO（组相对策略优化）
    - GiGPO（组内分组策略优化，步骤级）
- 基础模型：Qwen2.5-1.5B-Instruct、Qwen2.5-7B-Instruct、Qwen2.5-14B-Instruct（仅AppWorld）。

## 4. 资源与算力

- **论文明确说明**：
    - ALFWorld和WebShop实验使用**4×H100 GPU**，训练100轮（iteration）。
    - AppWorld实验使用**8×H100 GPU**，训练50轮。
    - 所有方法共享相同超参数，G2PO仅在优势计算阶段引入约1秒的额外CPU开销（仅占训练步总时间的0.4%），几乎无额外计算成本。
- **未明确说明**：具体训练时长（小时数）未给出，但可推测在合理范围内。

## 5. 实验数量与充分性

- **主要实验**：两个规模（1.5B、7B）在WebShop和ALFWorld上共进行4组主实验（每组3个随机种子取平均），加上AppWorld上14B实验（1组）。
- **消融实验**：在ALFWorld和WebShop上（1.5B模型）逐步添加节点中心优势、组聚合估计、边中心优势，验证各组件贡献（图3）。
- **超参数分析**：分析权重w的影响（图4a）。
- **方差分析**：统计状态价值方差分布（图4b），说明组聚合的合理性。
- **效率分析**：比较不同方法达到任务所需的推理步数（图4c）。
- **计算开销分析**：时间消耗分解（图5）。
- **组大小分析**：训练过程中平均组大小动态及分布（附录D图6、7）。
- **案例分析**：对比局部组比较与边中心优势（图11）。
- **充分性评价**：实验覆盖三种不同类型的长周期任务，模型规模从1.5B到14B，对比方法全面（包括离线和在线RL，提示方法，前沿大模型），消融和超参数分析完整。实验设计客观、公平（相同超参数、相同rollout设置）。

## 6. 主要结论与发现

- G2PO在三个基准上**一致且显著优于所有基线**。
    - 在WebShop上，相对于GRPO，成功率提升最高达22.2%（1.5B）；相对于GiGPO也有明显优势。
    - 在ALFWorld上，总体成功率提升14.4%（1.5B），各子任务平均提升显著。
    - 在AppWorld上，成功率提升2.8个百分点（相对于GRPO），在更复杂的任务中仍有效。
- **消融实验**：组聚合状态价值估计和边中心优势均带来显著增益，证实每个设计点的必要性。
- **效率优势**：G2PO训练的模型完成任务所需交互步数显著少于GRPO和GiGPO，降低了推理成本。
- **几乎零额外计算开销**：G2PO仅在CPU上进行图构建和优势计算，时间增加可忽略。
- 验证了图结构化视角对长周期智能体RL的有效性。

## 7. 优点

- **方法创新**：首次明确将线性轨迹重构为全局状态转移图，提出组聚合价值估计和边中心优势，从根本解决高方差和局部短视问题。
- **理论支撑**：提供了方差减少的数学证明（附录B），增强了方法的可信度。
- **实验充分**：多基准、多模型规模、多基线对比，消融和超参数分析详尽，结论可靠。
- **高效实用**：几乎不增加计算资源需求，适合大规模训练。
- **通用性**：适用于多种观察模态（文本、API返回），在AppWorld上有效，表明对复杂环境也有鲁棒性。
- **推理效率**：训练后的模型决策更高效，任务步数减少。

## 8. 不足与局限

- **实验覆盖**：仅基于verl-agent框架实现，未在其他RL框架（如RAGEN、Search-R1）上验证；未来可扩展到树采样、异步采样等场景。
- **应用限制**：依赖于环境观测的可聚类性（观测需可比较以分组）；对于高度动态、无重复状态的场景（如每个步骤观测均唯一），组聚合效果可能减弱。
- **超参数敏感性**：权重w需要调整（0.8最优），不同任务或模型可能需重新调参。
- **理论分析假设较强**：方差减少证明假设组内各步来自不同独立轨迹，实际中可能存在相关性。
- **仅关注稀疏奖励场景**：对于有密集中间奖励的任务，G2PO的优势可能不如稀疏奖励场景显著。

（完）
